<view class="loading-container {{show ? 'show' : ''}}" wx:if="{{show}}">
  <view class="loading-mask" wx:if="{{mask}}"></view>
  <view class="loading-content">
    <view class="loading-spinner"></view>
    <text class="loading-text">{{text}}</text>
  </view>
</view> 